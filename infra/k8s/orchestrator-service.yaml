apiVersion: v1
kind: Service
metadata:
  name: orchestrator
  namespace: mini-browser
  labels:
    app: orchestrator
spec:
  selector:
    app: orchestrator
  ports:
    - name: http
      port: 8090
      targetPort: 8090
      protocol: TCP
  type: ClusterIP
